{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
{% endblock stylesheets %}

{% block javascripts %}
    {{ parent() }}
    
{% endblock javascripts %}

{% block body %}
<button type="button" class="btn btn-primary mb-4" data-toggle="modal" data-target="#exampleModal"
        data-whatever="@mdo"><a href="{{ path('job.edit', {id: job.id}) }}"></a>
</button>
<div class="modal fade" id="exampleModal" tabindex="-1" role="modal"
     aria-labelledby="exampleModalLabel"
     aria-hidden="true"  >
    <div class="modal-dialog" role="modal" >
        <div class="modal-content" >


            <div class="modal-header">
                <h3 class="modal-title" id="exampleModalLabel">Ajouter Offre</h3>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
          <h1>Job creation</h1>

     <div class="row" >
                {{ form_start(formedit, {
                    'attr': {
                        'novalidate': 'novalidate',
                        'class': 'col-lg-5' 
                        }
                    })
                }}

                    {{ form_widget(formedit) }}
                   

                    <button type="submit" class="btn btn-primary">Créer</button>
                {{ form_end(formedit) }}
            </div>
            </div>
        </div>
    {#{{ form_end(form) }}#}
        <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Annuler</button>
                <button type="submit" class="btn btn-default"> <a href="{{ path('job.create') }}" class="btn btn-default navbar-btn"></a>Create</button>
            </div>
</form>            
        </div>
                
            </div>
            
        </div>
            {% endblock %}